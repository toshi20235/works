<!DOCTYPE html>
<html lang="ja">
<header>
   <meta charset="UTF-8" />
   
</header>
<body>
  <h1>
    Books
  </h1>
  <table>
    <thead>
      <tr>
       <th>Title</th>
       <th>Body</th>
        <th colspan="3"></th>
      </tr>
  </thead>
  
  <tbody>
      <% @books.each do |book| %>
       <tr>
          <td><%= book.title %></td>
          <td><%= book.body %></td>
          <td><a class="show" href=link_to book.title,book_path(book.id)>show</a></td>
       </tr>
      <% end %>
  </tbody>
  </table>
   <h2>New Book</h2>
  <%= form_with model: @book do |f| %>
   <h4>title</h4>
   <%= f.text_field :title %>
   <h4>body</h4>
   <%= f.text_area :body %>
    <%= f.submit 'Create Book' %>
  <% end %>
</body>